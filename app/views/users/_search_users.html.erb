<%= form_tag(users_path, :method => "get", id: "search-form") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search Users", data: {autocomplete_source: User.without_unverified_users.order(:name).map(&:name)} %>
  <%= submit_tag "Search", :name => nil %>
<% end %>
