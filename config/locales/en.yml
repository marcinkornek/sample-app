# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

en:
  hello: "Hello world"

  # helpers:
  #   label:
  #     session:
  #       email: 'Email/Username'

# własne errory
  # errors:
  #   messages:
  #     kotki: lol

  errors:
    messages:
      email_password: 'Invalid email/password or username/password combination'
      unverified: "User email is not verified. Please check your email to confirm registration. If you didn't got email click at link
      <a href='%{link}'>reactivate password</a>"


  activerecord:
    errors:
      models:
        user:
          attributes:
            email:
              taken: 'has already been taken. If this is your email address, try <a href="%{link}">reset password</a> instead.'
